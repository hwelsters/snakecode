{
  "name": "@snakecode/models",
  "version": "1.0.0",
  "description": "This sub-package represents the common models, to be used across Snakecode.",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build/ lib/ node_modules/",
    "lint": "eslint --fix --ext .ts src/"
  },
  "dependencies": {
  },
  "devDependencies": {
    "eslint-config-snakecode-base": "1.0.0"
  },
  "private": true
}
